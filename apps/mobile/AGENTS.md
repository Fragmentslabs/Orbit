# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Mobile x Desktop: feature gaps (rastreado)

## Pendente (fazer)
1. **Criação de MCP e Skills** (`tools.tsx` é só leitura — desktop tem McpSkillsPanel com add/edit/remove/import)
2. **Filtros do catálogo de modelos** — desktop ModelsView filtra por provider, capacidade, preço, velocidade, contexto

## Concluído
- FilePart nas mensagens do assistente (`case 'file'` em ChatAssistantMessage.tsx)
