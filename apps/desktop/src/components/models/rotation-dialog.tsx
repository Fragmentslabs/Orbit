/**
 * Reexporta o modal de rotação a partir do diretório dedicado.
 * Mantém a compatibilidade com o import path `@/src/components/models/rotation-dialog`
 * usado em `App.tsx`.
 */
export { RotationDialog, RotationDialogHost } from "./rotation"
