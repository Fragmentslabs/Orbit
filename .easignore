# EAS archive exclusions (monorepo root)
# only the Android app + workspace packages are needed to build the mobile APK

apps/desktop
docs
.claude
.tmp*
release
*.log
bf2*

# dependências (hoisted na raiz + por app)
node_modules

# projetos nativos gerados — EAS roda prebuild (CNG); /ios e /android são gitignored
apps/mobile/ios
apps/mobile/android

# estado local do Expo e artefatos TS
apps/mobile/.expo
.expo-export-check/
*.tsbuildinfo
